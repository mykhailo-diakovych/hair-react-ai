import styled from "styled-components";

export const StyledDeepArCanvas = styled.canvas`
  margin: 0 auto;
  width: auto;
  max-width: 100%;
  max-height: calc(100vh - 70px);
  touch-action: none;
`;
