import styled from "styled-components";
import { ButtonIcon } from "@components/Button/styled/ButtonIcon.styled";

export const ClientNavigateButton = styled(ButtonIcon)`
  position: absolute;
  top: 50%;
  left: 0;
  transform: translateY(-50%);
`;
