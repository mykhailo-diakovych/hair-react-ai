import styled from "styled-components";
import { Modal } from "@components/Modal/styled/Modal.styled";

export const SavedSimulationSettingsModal = styled(Modal)`
  .ant-modal-body {
    padding: 20px;
  }
`;
