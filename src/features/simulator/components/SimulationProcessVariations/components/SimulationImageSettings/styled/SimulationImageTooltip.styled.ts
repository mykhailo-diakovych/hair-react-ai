import styled from "styled-components";
import { Tooltip } from "antd";

export const SimulationImageTooltipStyled = styled(Tooltip)`
  position: absolute;
  top: 10px;
  right: 10px;

  // .ant-tooltip-inner {
  //   background-color: ${({ theme }) => theme.colors.primary};
  // }
`;
